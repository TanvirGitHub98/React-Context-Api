import React from "react";
const UseCallBack=({testFunForUseCallback})=>{

    console.log("Call back calling...")

    return(
        <div>

        </div>
    )
}
export default React.memo(UseCallBack);